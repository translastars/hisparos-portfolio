{
  "version": 2,
  "buildCommand": null,
  "installCommand": "npm ci --only=production",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" }
  ]
}
